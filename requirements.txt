# 驱动与核心
nonebot2[fastapi]>=2.3.0
nonebot-adapter-onebot>=2.4.3

# 调度
nonebot-plugin-apscheduler>=0.4.0

# HTTP 客户端（启用 HTTP/2 支持）
httpx[http2]>=0.24.1

# 可选：启用 Redis 持久化启用群列表时需要
redis>=5.0.0